{
  "id": "fix_code_formatting_prompt_markers_1",
  "suite": "capability",
  "desc": "The cluster contains two whole-answer code blocks that carry a `>>>` REPL prompt marker. House style: a code block or inline snippet is the code itself — a REPL or shell prompt marker (`>>>`, `...`, `$`, `In [1]:`) is chrome, not code, and a snippet that carries one is defective; the fix strips the marker and changes nothing else, keeping the code exactly as it was. The seed note's back block starts with `>>>`, so the marker must be stripped; note 2's back block starts with `>>>` too, so the marker must be stripped there as well; note 3 mentions the `.grad` attribute inline in its question and is already correct. All three notes share the 'python' and 'pytorch' tags, so the cluster surfaces through the agent's normal cluster exploration (tag and content queries); no exhaustive collection-wide search is needed. The agent should convert every defective block and leave note 3 untouched.",
  "seed_note_id": 1,
  "trials": 3,
  "max_steps": 15,
  "notes": [
    {
      "id": 1,
      "front": "In PyTorch, how is the gradient scaled by the parameter scale?",
      "back": "```python\n>>> grad.std() / param.std()\n```",
      "tags": ["python", "pytorch"],
      "notetype": "basic"
    },
    {
      "id": 2,
      "front": "In PyTorch, how is a tensor clipped to the range [0, 1]?",
      "back": "```python\n>>> t.clamp(0, 1)\n```",
      "tags": ["python", "pytorch"],
      "notetype": "basic"
    },
    {
      "id": 3,
      "front": "In PyTorch, what does the `.grad` attribute of a tensor hold?",
      "back": "The gradient of the tensor accumulated by autograd",
      "tags": ["python", "pytorch"],
      "notetype": "basic"
    }
  ],
  "expect": {
    "edits": [2, 2],
    "creates": [0, 0],
    "deletes": [0, 0],
    "must_touch": [1, 2],
    "must_not_touch": [3],
    "facts": ["grad.std() / param.std()", "t.clamp(0, 1)"],
    "must_not_contain": [">>>"]
  },
  "judge_assertions": [
    "Every proposed note whose whole answer is code writes that answer as a fenced code block with a language tag containing only the code itself — the `>>>` REPL prompt marker is stripped and nothing else about the code is changed.",
    "The card whose question mentions the `.grad` attribute in inline code stays exactly as it is: a mention inside a question remains inline and untouched, and no note other than the two with the prompt-marker defect is changed."
  ],
  "reference": [
    {
      "type": "edit",
      "note_id": 1,
      "back": "```python\ngrad.std() / param.std()\n```",
      "rationale": "strip the `>>>` REPL prompt marker from the block; the code itself is unchanged"
    },
    {
      "type": "edit",
      "note_id": 2,
      "back": "```python\nt.clamp(0, 1)\n```",
      "rationale": "strip the `>>>` REPL prompt marker from the block; the code itself is unchanged"
    }
  ]
}